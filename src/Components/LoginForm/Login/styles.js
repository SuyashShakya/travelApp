import {makeStyles} from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    loginText : {
        textAlign: 'center'
    },
    orText: {
        textAlign: 'center'
    }
}))

export default useStyles;